1) Write a program to get two integers from the user and print the sum of the two integers?

#include <stdio.h>
int main()
{
    int n1,n2,sum;
    printf("Enter the first Number: ");
    scanf("%d",&n1);
    printf("Enter the second Number: ");
    scanf("%d",&n2);
    sum=n1+n2;
    printf("\n Sum of n1 and n2 is %d",sum);
    return 0;
}

2) Write a function to add two integers?

#include <stdio.h>
int sum(int a,int b);
int main()
{
    int n1,n2,add;
    printf("Enter the first Number: ");
    scanf("%d",&n1);
    printf("Enter the second Number: ");
    scanf("%d",&n2);
    add=sum(n1,n2);
    printf("sum is %d",add);
}
int sum(int a,int b)
{
    int add;
    add=a+b;
    return add;
}
